server:
  port: 9020
  servlet:
    context-path: /workspace

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/lovable-workspace-db
    username: postgres
    password: hello1234
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379

minio:
  url: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin123
  bucket: projects
